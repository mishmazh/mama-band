import cls from './NotFoundPage.module.scss';

const NotFoundPage = () => {
    return (
        <div className={cls.notFound}>
            <div className={cls.item}>404</div>
        </div>
    );
};

export default NotFoundPage;
